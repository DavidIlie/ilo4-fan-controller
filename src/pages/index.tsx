const Home = (): JSX.Element => {
    return (
        <div className="h-screen bg-gray-800 flex justify-center items-center">
            <h1 className="text-5xl font-semibold text-white">Hello There</h1>
        </div>
    );
};

export default Home;
